/**
 * Same-origin default so the hero always loads (no hotlink/CSP issues).
 * Host `imageUrl` overrides when set in admin.
 */
export const DEFAULT_QUESTION_HERO_URL = "/question-hero-default.svg";
